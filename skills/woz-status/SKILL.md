---
name: woz-status
description: Deprecated alias — use `/woz status`.
disable-model-invocation: true
---

`/woz-status` was renamed. Invoke the **woz** skill and run its `status` subcommand (equivalent to `/woz status`).
