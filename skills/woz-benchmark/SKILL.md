---
name: woz-benchmark
description: Deprecated alias — use `/woz benchmark`.
disable-model-invocation: true
---

`/woz-benchmark` was renamed. Invoke the **woz** skill and run its `benchmark` subcommand (equivalent to `/woz benchmark`).
